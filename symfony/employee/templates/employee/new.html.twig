{% extends 'base.html.twig' %}

{% block title %}New Employee{% endblock %}

{% block body %}
<div class="container">
    <h2 class="text-center mt-5 mb-3">Add New Employee</h2>
    <div class="card">
        <div class="card-header">
            <a href="{{ path('app_employee_index') }}" class="btn  btn-outline-info">View All Employee</a>
        </div>
        <div class="card-body">

    {{ include('employee/_form.html.twig') }}
        </div>
    </div>

</div>
    <a href="{{ path('app_employee_index') }}">Back to list</a>

{% endblock %}
