<sw-page>
    <template #smart-bar-header>
        <h2>{{ isNew ? 'Create' : 'Edit' }} Blog</h2>
    </template>

    <template #smart-bar-actions>
        <sw-button @click="onCancel">Cancel</sw-button>

        <sw-button-process
                :is-loading="isLoading"
                v-model:process-success="isSaveSuccessful"
                variant="primary"
                @click="onSave"
        >
            Save
        </sw-button-process>
    </template>

    <template #language-switch>
        <sw-language-switch
                :save-changes-function="saveOnLanguageChange"
                @on-change="onChangeLanguage"
        >
        </sw-language-switch>
    </template>

    <template #content>
        <sw-card title="General" v-if="blog">
            <sw-text-field
                    v-model:value="blog.name"
                    label="Name"
                    required
            >
            </sw-text-field>
            <sw-textarea-field
                    v-model:value="blog.description"
                    label="Description"
            >
            </sw-textarea-field>
            <sw-switch-field
                    v-model:value="blog.active"
                    label="Active"
            >
            </sw-switch-field>
            <sw-text-field
                    v-model:value="blog.authorId"
                    label="Author"
            >
            </sw-text-field>
            <sw-datepicker
                    v-model:value="blog.releaseDate"
                    label="Release Date"
            >
            </sw-datepicker>
            <sw-entity-multi-select
                    v-model:value="blog.categories"
                    entity="blog_category"
                    label="Blog Categories"
            >
            </sw-entity-multi-select>
            <sw-entity-multi-select
                    v-model:value="blog.products"
                    entity="product"
                    label="Products"
            >
            </sw-entity-multi-select>

        </sw-card>
    </template>
</sw-page>